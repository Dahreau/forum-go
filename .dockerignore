# Exclude version control files
.git
.gitignore

# Exclude Go workspace cache and module files
/pkg
/bin

# Exclude editor and IDE settings
*.swp
*.swo
*.idea/
.vscode/
*.iml

# Exclude compiled binaries and executables
*.exe
*.out
*.so
*.o
*.a

# Exclude log files and temporary files
*.log
*.tmp
*.bak

# Exclude Dockerfile itself (not needed in the image)
Dockerfile

# Exclude Docker Compose files (only needed for orchestration)
docker-compose.yml

# Exclude database files (you may not want to include local DB files)
*.db
*.sqlite

# Exclude test results or temporary Go files
*.test
